<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.unlam.edu.ar.videotecamoviltp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".VideotecaApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Videotecamoviltp">
        <activity android:name=".SplashActivity" android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SignUpActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Videotecamoviltp.NoActionBar" />
        <activity
            android:name=".FavActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Videotecamoviltp.NoActionBar" />
        <activity
            android:name=".LogInActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Videotecamoviltp.NoActionBar" />
        <activity
            android:name=".MovieDetailsActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Videotecamoviltp.NoActionBar" />
        <activity
            android:name=".UserActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Videotecamoviltp.NoActionBar" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".HomeActivity"></activity>
    </application>

</manifest>